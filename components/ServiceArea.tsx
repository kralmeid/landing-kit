// Optional service-area / coverage section.
//
// Enable by exporting markup from this component and importing it into
// app/[locale]/page.tsx after <Trust />. Copy lives in messages.serviceArea.*.
export function ServiceArea() {
  return null;
}
